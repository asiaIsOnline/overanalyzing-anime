---
import { Icon } from 'astro-icon/components'

interface Props {
	title: string;
	body: string;
}

const { title, body } = Astro.props;
---

<div>
    <h2 class="text-3xl">{title}</h2>
    <p>{body}</p>
</div>
